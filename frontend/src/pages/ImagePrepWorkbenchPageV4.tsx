import { useEffect, useMemo, useRef, useState } from "react";
import { useNavigate } from "react-router-dom";
import {
  acceptFaceCrops,
  createManualCrop,
  deleteDerivativeAsset,
  getImagePrepState,
  getProjectRevision,
  inspectDataset,
  preparedAssetPreviewUrl,
  proposeFaceCrops,
  setAssetImageTransform,
  setGlobalImageTransform,
  setImageInclusion,
  setTrainingResolution,
  type FaceCropProposal,
  type ImagePrepState,
  type ImageTransform,
  type ProjectAsset,
  type ResolutionAsset,
  type TrainingResolutionPolicy,
} from "../api";
import { useSession } from "../session";

type Filter = "all" | "included" | "excluded";
type CropRect = { x: number; y: number; width: number; height: number };
type DragMode = "move" | "nw" | "ne" | "sw" | "se" | null;

const ASPECTS = ["source", "16:9", "1:1", "4:5", "5:4", "9:16"];
const DERIVATIVE_ASPECTS = ["1:1", "4:5", "5:4", "9:16", "16:9"];
const MP_PRESETS = [0.25, 0.5, 0.75, 1, 1.5];
const DEFAULT_GLOBAL: ImageTransform = { aspect_ratio: "source", crop_mode: "fit", crop_x: 0.5, crop_y: 0.5, exposure: 0, brightness: 0, contrast: 0, gamma: 1 };

function aspectNumber(value: string, fallback = 1) {
  if (value === "source") return fallback;
  const [w, h] = value.split(":").map(Number);
  return w > 0 && h > 0 ? w / h : fallback;
}
function sameJson(a: unknown, b: unknown) { return JSON.stringify(a ?? {}) === JSON.stringify(b ?? {}); }
function bucketKey(row: ResolutionAsset) { return `${row.bucket_width}×${row.bucket_height}`; }
function bucketCount(rows: ResolutionAsset[]) { return new Set(rows.map(bucketKey)).size; }
function cropLoss(row: ResolutionAsset) { return Math.max(0, 1 - (row.crop_width * row.crop_height) / Math.max(1, row.file_width * row.file_height)); }
function derivativeBaseTransform(asset: ProjectAsset): ImageTransform {
  const operation = [...(asset.operations || [])].reverse().find((item) => item.type === "manual_crop" || item.type === "face_crop");
  const aspect = typeof operation?.aspect_ratio === "string" ? operation.aspect_ratio : "source";
  return { aspect_ratio: aspect, crop_mode: "fit", crop_x: 0.5, crop_y: 0.5, crop_scale: 1, exposure: 0, brightness: 0, contrast: 0, gamma: 1 };
}
function inheritedTransform(globalTransform: ImageTransform, asset: ProjectAsset): ImageTransform { return asset.baked_transform ? derivativeBaseTransform(asset) : globalTransform; }
function mergedTransform(globalTransform: ImageTransform, asset: ProjectAsset) { return { ...inheritedTransform(globalTransform, asset), ...(asset.transform_override || {}) }; }
function previewFilter(transform: ImageTransform) {
  const exposure = transform.exposure ?? 0, brightness = transform.brightness ?? 0, contrast = transform.contrast ?? 0, gamma = transform.gamma ?? 1;
  const gammaPreview = Math.pow(1.18, 1 - gamma);
  return `brightness(${Math.max(0, (1 + brightness) * Math.pow(2, exposure) * gammaPreview)}) contrast(${Math.max(0, 1 + contrast)})`;
}
function transformSummary(transform: ImageTransform, inherited?: ImageTransform) {
  const base = inherited || {}, bits: string[] = [], aspect = transform.aspect_ratio;
  if (aspect && aspect !== (base.aspect_ratio || "source")) bits.push(`aspect ${aspect}`);
  const values: Array<[keyof ImageTransform, string, number]> = [["exposure", "exp", 0], ["brightness", "bright", 0], ["contrast", "contrast", 0], ["gamma", "gamma", 1]];
  for (const [key, label, fallback] of values) {
    const value = transform[key] as number | undefined, inheritedValue = (base[key] as number | undefined) ?? fallback;
    if (value !== undefined && Math.abs(value - inheritedValue) > 1e-6) bits.push(`${label} ${value > 0 && key !== "gamma" ? "+" : ""}${Number(value).toFixed(key === "exposure" ? 1 : 2)}`);
  }
  if ((transform.crop_scale ?? 1) < 0.999) bits.push(`crop ${Math.round((transform.crop_scale ?? 1) * 100)}%`);
  return bits;
}
function SliderField({ label, value, min, max, step, onChange }: { label: string; value: number; min: number; max: number; step: number; onChange: (value: number) => void }) {
  const precision = String(step).includes(".") ? String(step).split(".")[1].length : 0;
  const clamp = (v: number) => Math.max(min, Math.min(max, Number(v.toFixed(precision))));
  return <label className="tuning-slider"><span className="slider-title"><span>{label}</span><strong>{value.toFixed(precision)}</strong></span><span className="slider-row"><button type="button" className="micro-button" onClick={() => onChange(clamp(value - step))}>−</button><input type="range" min={min} max={max} step={step} value={value} onChange={(event) => onChange(Number(event.target.value))} /><button type="button" className="micro-button" onClick={() => onChange(clamp(value + step))}>+</button></span></label>;
}
function cropFromCenter(aspect: string, imageWidth: number, imageHeight: number, scale = 0.8): CropRect {
  const sourceAspect = imageWidth / Math.max(1, imageHeight), target = aspectNumber(aspect, sourceAspect); let baseW = 1, baseH = 1;
  if (sourceAspect >= target) baseW = target / sourceAspect; else baseH = sourceAspect / target;
  const width = baseW * scale, height = baseH * scale; return { x: (1 - width) / 2, y: (1 - height) / 2, width, height };
}
function clampCrop(rect: CropRect, aspect: string, imageWidth: number, imageHeight: number): CropRect {
  const sourceAspect = imageWidth / Math.max(1, imageHeight), target = aspectNumber(aspect, sourceAspect); let width = Math.max(0.04, Math.min(1, rect.width)); let height = (width * sourceAspect) / target;
  if (height > 1) { height = 1; width = (height * target) / sourceAspect; }
  return { x: Math.max(0, Math.min(1 - width, rect.x)), y: Math.max(0, Math.min(1 - height, rect.y)), width, height };
}
function ManualCropEditor({ imageUrl, filename, aspect, onCreate, busy }: { imageUrl: string; filename: string; aspect: string; onCreate: (crop: CropRect) => void; busy: boolean }) {
  const ref = useRef<HTMLDivElement | null>(null); const [dims, setDims] = useState({ width: 1, height: 1 }); const [crop, setCrop] = useState<CropRect>(() => cropFromCenter(aspect, 1, 1)); const [drag, setDrag] = useState<{ mode: DragMode; x: number; y: number; start: CropRect } | null>(null);
  useEffect(() => setCrop(cropFromCenter(aspect, dims.width, dims.height)), [aspect, dims.width, dims.height, filename]);
  function point(event: React.PointerEvent) { const bounds = ref.current!.getBoundingClientRect(); return { x: (event.clientX - bounds.left) / bounds.width, y: (event.clientY - bounds.top) / bounds.height }; }
  function begin(event: React.PointerEvent, mode: DragMode) { event.preventDefault(); event.stopPropagation(); event.currentTarget.setPointerCapture(event.pointerId); const p = point(event); setDrag({ mode, x: p.x, y: p.y, start: crop }); }
  function move(event: React.PointerEvent) { if (!drag) return; const p = point(event), dx = p.x - drag.x, dy = p.y - drag.y; if (drag.mode === "move") { setCrop(clampCrop({ ...drag.start, x: drag.start.x + dx, y: drag.start.y + dy }, aspect, dims.width, dims.height)); return; } const sign = drag.mode?.includes("w") ? -1 : 1, width = Math.max(0.04, drag.start.width + dx * sign), right = drag.start.x + drag.start.width, bottom = drag.start.y + drag.start.height; let next = clampCrop({ x: drag.mode?.includes("w") ? right - width : drag.start.x, y: drag.start.y, width, height: drag.start.height }, aspect, dims.width, dims.height); if (drag.mode?.includes("n")) next = clampCrop({ ...next, y: bottom - next.height }, aspect, dims.width, dims.height); setCrop(next); }
  const pxW = Math.max(1, Math.round(crop.width * dims.width)), pxH = Math.max(1, Math.round(crop.height * dims.height));
  return <div className="manual-editor-shell"><div ref={ref} className="manual-crop-canvas" style={{ aspectRatio: `${dims.width}/${dims.height}` }} onPointerMove={move} onPointerUp={() => setDrag(null)} onPointerCancel={() => setDrag(null)}><img src={imageUrl} alt={filename} draggable={false} onLoad={(event) => setDims({ width: event.currentTarget.naturalWidth, height: event.currentTarget.naturalHeight })} /><div className="manual-source-size">Prepared {dims.width}×{dims.height}</div><div className="interactive-crop-box" style={{ left: `${crop.x * 100}%`, top: `${crop.y * 100}%`, width: `${crop.width * 100}%`, height: `${crop.height * 100}%` }} onPointerDown={(event) => begin(event, "move")}><span className="crop-size-label">{pxW}×{pxH} · {((pxW * pxH) / 1e6).toFixed(2)} MP</span>{(["nw", "ne", "sw", "se"] as const).map((corner) => <span key={corner} className={`crop-handle ${corner}`} onPointerDown={(event) => begin(event, corner)} />)}</div></div><button className="primary" disabled={busy} onClick={() => onCreate(crop)}>Create {aspect} derivative</button></div>;
}
function cropGeometry(width: number, height: number, aspect: string, scale: number, cropX: number, cropY: number): CropRect { if (aspect === "source") return { x: 0, y: 0, width: 1, height: 1 }; const source = width / Math.max(1, height), target = aspectNumber(aspect, source); let baseW = 1, baseH = 1; if (source >= target) baseW = target / source; else baseH = source / target; const w = baseW * scale, h = baseH * scale; return { x: (1 - w) * cropX, y: (1 - h) * cropY, width: w, height: h }; }
function PositionCropEditor({ imageUrl, filename, aspect, scale, cropX, cropY, onChange, filter }: { imageUrl: string; filename: string; aspect: string; scale: number; cropX: number; cropY: number; onChange: (next: { x: number; y: number; scale: number }) => void; filter: string }) {
  const ref = useRef<HTMLDivElement | null>(null); const [dims, setDims] = useState({ width: 1, height: 1 }); const [drag, setDrag] = useState<{ mode: DragMode; cx: number; cy: number; x: number; y: number; scale: number } | null>(null); const crop = cropGeometry(dims.width, dims.height, aspect, scale, cropX, cropY); const pxW = Math.round(crop.width * dims.width), pxH = Math.round(crop.height * dims.height);
  function begin(event: React.PointerEvent, mode: DragMode) { if (aspect === "source") return; event.preventDefault(); event.stopPropagation(); event.currentTarget.setPointerCapture(event.pointerId); setDrag({ mode, cx: event.clientX, cy: event.clientY, x: cropX, y: cropY, scale }); }
  function move(event: React.PointerEvent) { if (!drag || !ref.current || aspect === "source") return; const bounds = ref.current.getBoundingClientRect(), dx = (event.clientX - drag.cx) / bounds.width, dy = (event.clientY - drag.cy) / bounds.height; if (drag.mode === "move") { onChange({ x: Math.max(0, Math.min(1, drag.x + dx / Math.max(0.001, 1 - crop.width))), y: Math.max(0, Math.min(1, drag.y + dy / Math.max(0.001, 1 - crop.height))), scale: drag.scale }); return; } const sx = drag.mode?.includes("w") ? -dx : dx, sy = drag.mode?.includes("n") ? -dy : dy, delta = Math.abs(sx) > Math.abs(sy) ? sx : sy; onChange({ x: drag.x, y: drag.y, scale: Math.max(0.1, Math.min(1, drag.scale + delta * 2)) }); }
  return <div className="position-crop-shell"><div ref={ref} className="position-crop-canvas" style={{ aspectRatio: `${dims.width}/${dims.height}` }} onPointerMove={move} onPointerUp={() => setDrag(null)} onPointerCancel={() => setDrag(null)}><img src={imageUrl} alt={filename} draggable={false} style={{ filter }} onLoad={(event) => setDims({ width: event.currentTarget.naturalWidth, height: event.currentTarget.naturalHeight })} />{aspect !== "source" ? <div className="position-crop-box" style={{ left: `${crop.x * 100}%`, top: `${crop.y * 100}%`, width: `${crop.width * 100}%`, height: `${crop.height * 100}%` }} onPointerDown={(event) => begin(event, "move")}><span>{pxW}×{pxH} · {((pxW * pxH) / 1e6).toFixed(2)} MP</span>{(["nw", "ne", "sw", "se"] as const).map((corner) => <i key={corner} className={`crop-handle ${corner}`} onPointerDown={(event) => begin(event, corner)} />)}</div> : <div className="position-crop-full-label">Full source · {dims.width}×{dims.height}</div>}</div><small>{aspect === "source" ? "No composition crop." : "Drag the window to reposition it; drag a corner to resize it."}</small></div>;
}
function FaceProposalCard({ proposal, imageUrl, selected, onToggle }: { proposal: FaceCropProposal; imageUrl: string; selected: boolean; onToggle: () => void }) {
  const [x1, y1, x2, y2] = proposal.crop_box, cropW = x2 - x1, cropH = y2 - y1, mp = cropW * cropH / 1e6, crop = proposal.normalized_crop;
  return <button className={`prep-image-card face-proposal-card ${selected ? "selected" : ""}`} onClick={onToggle}><div className="face-proposal-native" style={{ aspectRatio: `${proposal.source_width}/${proposal.source_height}` }}><img src={imageUrl} alt={proposal.filename} /><div className="face-crop-box" style={{ left: `${crop.x * 100}%`, top: `${crop.y * 100}%`, width: `${crop.width * 100}%`, height: `${crop.height * 100}%` }} /></div><div className="prep-image-meta"><strong>{proposal.filename}</strong><span>Face {proposal.face_index + 1} · score {proposal.score}</span><span>{proposal.aspect_ratio} · {proposal.padding_percent}% padding</span><span className={mp < 0.5 ? "status-suspect" : "status-good"}>Crop {cropW}×{cropH} · {mp.toFixed(2)} MP</span></div></button>;
}
function BucketPreview({ rows, onReview }: { rows: ResolutionAsset[]; onReview: (filename: string) => void }) { const groups = useMemo(() => { const map = new Map<string, string[]>(); for (const row of rows) map.set(bucketKey(row), [...(map.get(bucketKey(row)) || []), row.filename]); return [...map.entries()].sort((a, b) => b[1].length - a[1].length); }, [rows]); const significant = rows.filter((row) => cropLoss(row) > 0.05); return <div className="bucket-preview stack"><div className="prep-section-heading"><div><div className="card-title">Expected bucket groups</div><p className="muted">Preview only; exact trainer assignments are confirmed after caching.</p></div><div className="bucket-summary"><strong>{groups.length}</strong><span>groups</span><strong>{rows.length}</strong><span>images</span></div></div><div className="bucket-group-grid">{groups.map(([key, names]) => <details className="bucket-group" key={key}><summary><strong>{key}</strong><span>{names.length} image{names.length === 1 ? "" : "s"}</span></summary><div className="bucket-file-list">{names.map((name) => <span key={name}>{name}</span>)}</div></details>)}</div>{significant.length > 0 && <div className="notice"><strong>{significant.length}</strong> image{significant.length === 1 ? "" : "s"} lose more than 5% to composition cropping.<div className="crop-review-actions">{significant.map((row) => <button className="secondary" key={row.filename} onClick={() => onReview(row.filename)}>Review {row.filename}</button>)}</div></div>}</div>; }

export function ImagePrepWorkbenchPageV4() {
  const navigate = useNavigate(); const { project, revision, setRevision, dataset, setDataset, modelFamily } = useSession();
  const [prep, setPrep] = useState<ImagePrepState | null>(null), [selected, setSelected] = useState<Set<string>>(new Set()), [filter, setFilter] = useState<Filter>("all"), [busy, setBusy] = useState(false), [toast, setToast] = useState(""), [toastKind, setToastKind] = useState<"ok" | "warn" | "error">("ok"), [focusName, setFocusName] = useState(""), [editName, setEditName] = useState(""), [globalDraft, setGlobalDraft] = useState<ImageTransform>({}), [resolutionDraft, setResolutionDraft] = useState<TrainingResolutionPolicy>({ max_megapixels: 1, enable_bucket: true, bucket_no_upscale: true, dimension_step: 16 }), [overrideDraft, setOverrideDraft] = useState<ImageTransform>({}), [manualAspect, setManualAspect] = useState("1:1"), [faceAspect, setFaceAspect] = useState("1:1"), [facePadding, setFacePadding] = useState(60), [faceProposals, setFaceProposals] = useState<FaceCropProposal[]>([]), [accepted, setAccepted] = useState<Set<string>>(new Set()), [previewVersion, setPreviewVersion] = useState(0);
  function notify(message: string, kind: "ok" | "warn" | "error" = "ok") { setToast(""); requestAnimationFrame(() => { setToastKind(kind); setToast(message); }); }
  useEffect(() => { if (!toast) return; const timer = setTimeout(() => setToast(""), 5000); return () => clearTimeout(timer); }, [toast]);
  async function load() { if (!project || !revision) return; const state = await getImagePrepState(project.id, revision.id); setPrep(state); setGlobalDraft(state.global_transform); setResolutionDraft(state.training_resolution); const firstSource = state.assets.find((asset) => asset.asset_kind !== "derived" && asset.included !== false)?.filename || ""; setFocusName((current) => state.assets.some((asset) => asset.filename === current && asset.asset_kind !== "derived" && asset.included !== false) ? current : firstSource); }
  useEffect(() => { load().catch((error) => notify(error instanceof Error ? error.message : "Unable to load prep", "error")); }, [project, revision?.id]);
  const assets = prep?.assets ?? revision?.assets ?? [], included = assets.filter((asset) => asset.included !== false), sources = assets.filter((asset) => asset.asset_kind !== "derived"), workingSources = included.filter((asset) => asset.asset_kind !== "derived"), derivatives = included.filter((asset) => asset.asset_kind === "derived"), faceDerived = derivatives.filter((asset) => asset.origin === "face"), manualDerived = derivatives.filter((asset) => asset.origin === "manual"), sourceOverrides = workingSources.filter((asset) => Object.keys(asset.transform_override || {}).length > 0), derivedOverrides = derivatives.filter((asset) => transformSummary(asset.transform_override || {}, asset.baked_transform ? derivativeBaseTransform(asset) : {}).length > 0), rows = prep?.resolution_assets ?? [], visibleSources = sources.filter((asset) => filter === "all" ? true : filter === "included" ? asset.included !== false : asset.included === false);
  const globalRecipeActive = transformSummary(prep?.global_transform || {}, DEFAULT_GLOBAL).length > 0;
  const focus = workingSources.find((asset) => asset.filename === focusName) ?? workingSources[0]; const focusImage = dataset?.images.find((image) => image.filename === focus?.filename); const focusIndex = focus ? workingSources.findIndex((asset) => asset.filename === focus.filename) : -1; const editAsset = included.find((asset) => asset.filename === editName); const globalDirty = !sameJson(globalDraft, prep?.global_transform), resolutionDirty = !sameJson(resolutionDraft, prep?.training_resolution), overrideDirty = !sameJson(overrideDraft, editAsset?.transform_override ?? {}), targetAspect = globalDraft.aspect_ratio || "source";
  const preparedPreview = (filename: string) => `${preparedAssetPreviewUrl(project!.id, revision!.id, filename)}?v=${previewVersion}`;
  async function refresh() { if (!project || !revision) return null; const nextRevision = await getProjectRevision(project.id, revision.id); setRevision(nextRevision); const state = await getImagePrepState(project.id, revision.id); setPrep(state); setGlobalDraft(state.global_transform); setResolutionDraft(state.training_resolution); setDataset(await inspectDataset(nextRevision.files_path)); setPreviewVersion((value) => value + 1); return state; }
  function toggle(filename: string) { setSelected((current) => { const next = new Set(current); next.has(filename) ? next.delete(filename) : next.add(filename); return next; }); }
  async function inclusion(value: boolean) { if (!project || !revision || !selected.size) return; const count = selected.size; setBusy(true); try { await setImageInclusion(project.id, revision.id, [...selected], value); setSelected(new Set()); await refresh(); notify(`${count} source image(s) ${value ? "included" : "excluded"}.`); } finally { setBusy(false); } }
  async function all(value: boolean) { if (!project || !revision) return; setBusy(true); try { await setImageInclusion(project.id, revision.id, sources.map((asset) => asset.filename), value); setSelected(new Set()); await refresh(); notify(value ? "All source images included." : "All source images excluded."); } finally { setBusy(false); } }
  function step(delta: number) { if (!workingSources.length) return; setFocusName(workingSources[(focusIndex + delta + workingSources.length) % workingSources.length].filename); }
  async function saveGlobal() { if (!project || !revision || !globalDirty) return; const before = bucketCount(rows); setBusy(true); try { const state = await setGlobalImageTransform(project.id, revision.id, globalDraft); setPrep(state); setGlobalDraft(state.global_transform); setPreviewVersion((value) => value + 1); const after = bucketCount(state.resolution_assets); notify(after !== before ? `Bucket layout changed: ${before} → ${after} expected groups.` : "Base composition saved.", after !== before ? "warn" : "ok"); } finally { setBusy(false); } }
  async function saveResolution() { if (!project || !revision || !resolutionDirty) return; const before = bucketCount(rows); setBusy(true); try { const state = await setTrainingResolution(project.id, revision.id, resolutionDraft); setPrep(state); setResolutionDraft(state.training_resolution); const after = bucketCount(state.resolution_assets); notify(after !== before ? `Bucket layout changed: ${before} → ${after} expected groups.` : "Training resolution saved.", after !== before ? "warn" : "ok"); } finally { setBusy(false); } }
  async function addManual(crop: CropRect) { if (!project || !revision || !focus) return; setBusy(true); try { await createManualCrop(project.id, revision.id, focus.filename, manualAspect, crop); await refresh(); notify(`Manual ${manualAspect} derivative created from ${focus.filename}.`); } finally { setBusy(false); } }
  async function detect() { if (!project || !revision) return; setBusy(true); try { const names = selected.size ? [...selected].filter((name) => workingSources.some((asset) => asset.filename === name)) : workingSources.map((asset) => asset.filename); const result = await proposeFaceCrops(project.id, revision.id, names, faceAspect, facePadding); setFaceProposals(result.proposals); setAccepted(new Set(result.proposals.map((proposal) => proposal.id))); notify(`${result.proposals.length} face proposal(s) ready from saved prepared sources.`); } catch (error) { notify(error instanceof Error ? error.message : "Face detection failed", "error"); } finally { setBusy(false); } }
  async function addFaces() { if (!project || !revision) return; const chosen = faceProposals.filter((proposal) => accepted.has(proposal.id)); if (!chosen.length) return; setBusy(true); try { const result = await acceptFaceCrops(project.id, revision.id, chosen); setFaceProposals([]); setAccepted(new Set()); await refresh(); notify(`${result.assets.length} face derivative(s) added.`); } finally { setBusy(false); } }
  async function removeDerivative(asset: ProjectAsset) { if (!project || !revision || asset.asset_kind !== "derived") return; if (!window.confirm(`Remove project derivative ${asset.filename}?\n\nThe external source is unaffected.`)) return; setBusy(true); try { await deleteDerivativeAsset(project.id, revision.id, asset.filename); if (editName === asset.filename) { setEditName(""); setOverrideDraft({}); } await refresh(); notify(`${asset.filename} removed from this project.`); } catch (error) { notify(error instanceof Error ? error.message : "Unable to remove derivative", "error"); } finally { setBusy(false); } }
  function openEditor(filename: string) { const asset = included.find((candidate) => candidate.filename === filename); if (!asset) return; if (editName === filename) { setEditName(""); return; } setEditName(filename); setOverrideDraft(asset.transform_override || {}); }
  function review(filename: string) { openEditor(filename); requestAnimationFrame(() => document.getElementById("per-image-v4")?.scrollIntoView({ behavior: "smooth", block: "start" })); }
  async function saveOverride() { if (!project || !revision || !editAsset || !overrideDirty) return; const before = bucketCount(rows); setBusy(true); try { const state = await setAssetImageTransform(project.id, revision.id, editAsset.filename, overrideDraft); setPrep(state); const after = bucketCount(state.resolution_assets); await refresh(); notify(after !== before ? `Bucket layout changed: ${before} → ${after} expected groups after ${editAsset.filename}.` : `${editAsset.filename} override saved.`, after !== before ? "warn" : "ok"); } finally { setBusy(false); } }
  async function resetOverride() { if (!project || !revision || !editAsset) return; const reset = editAsset.baked_transform ? derivativeBaseTransform(editAsset) : {}; setBusy(true); try { await setAssetImageTransform(project.id, revision.id, editAsset.filename, reset); setOverrideDraft(reset); await refresh(); notify(editAsset.baked_transform ? "Derivative adjustment reset to its baked prepared image." : "Image override reset."); } finally { setBusy(false); } }
  if (!project || !revision || !dataset) return <section className="panel"><h1>Image Prep</h1><p>Open a project first.</p></section>;
  const down = rows.filter((row) => row.direction === "downscale").length, preserved = rows.filter((row) => row.direction === "native").length, low = rows.filter((row) => row.low_detail).length, cropReview = rows.filter((row) => cropLoss(row) > 0.05).length, groups = [...new Set(rows.map(bucketKey))], missing = included.filter((asset) => !asset.caption.trim()).length;
  return <div className="stack image-prep-page image-prep-v3 image-prep-v4">
    {toast && <div className={`prep-toast ${toastKind}`}>{toast}</div>}
    <header className="page-header"><div><p className="eyebrow">{project.name} · {project.id} · {revision.name}</p><h1>Build the Training Dataset</h1><p className="muted">Saved source preparation is reused consistently for derivative creation, review, and training.</p></div><div className="prep-counts provenance-counts"><span><strong>{sources.length}</strong> sources</span><span className="status-good"><strong>{included.length}</strong> working</span><span><strong>{derivatives.length}</strong> derived</span><span><strong>{sourceOverrides.length + derivedOverrides.length}</strong> exceptions</span><span className={globalRecipeActive ? "status-good" : ""}><strong>{globalRecipeActive ? "On" : "Off"}</strong> global recipe</span></div></header>
    <section className="panel stack"><div className="prep-section-heading"><div><p className="eyebrow">Stage 1</p><div className="card-title">Working Assets</div><p className="muted">Choose source images for this project. Saved source preparation is visible on every included source card.</p></div><div className="actions"><button className="secondary" onClick={() => all(true)}>Include all sources</button><button className="secondary" onClick={() => all(false)}>Exclude all sources</button></div></div><div className="segmented"><button className={filter === "all" ? "selected" : ""} onClick={() => setFilter("all")}>All {sources.length}</button><button className={filter === "included" ? "selected" : ""} onClick={() => setFilter("included")}>Working {workingSources.length}</button><button className={filter === "excluded" ? "selected" : ""} onClick={() => setFilter("excluded")}>Excluded {sources.length - workingSources.length}</button></div><div className="asset-subheading">Base Project Assets <span>{visibleSources.length}</span></div><div className="prep-grid">{visibleSources.map((asset) => { const rawImage = dataset.images.find((image) => image.filename === asset.filename)?.image_url; const live = asset.included !== false && globalDirty; return <div className={`prep-image-card ${focusName === asset.filename ? "focused" : ""} ${asset.included === false ? "excluded" : ""}`} key={asset.filename}><button className="prep-focus-button" onClick={() => asset.included !== false && setFocusName(asset.filename)}><div className="prep-image-wrap"><img src={live ? rawImage : asset.included !== false ? preparedPreview(asset.filename) : rawImage} alt={asset.filename} style={live ? { filter: previewFilter(globalDraft) } : undefined} />{asset.included !== false && !globalDirty && globalRecipeActive && <span className="transform-badge">Prepared</span>}{live && <span className="transform-badge">Preview</span>}</div><div className="prep-image-meta"><strong>{asset.filename}</strong>{asset.included !== false && <span>{live ? "Unsaved global preview" : globalRecipeActive ? "Global recipe" : "Base asset"}</span>}</div></button><button className={`batch-check ${selected.has(asset.filename) ? "checked" : ""}`} onClick={() => toggle(asset.filename)}>{selected.has(asset.filename) ? "✓" : ""}</button></div>; })}</div>{derivatives.length > 0 && <><div className="asset-subheading">Project derivatives <span>{faceDerived.length} face · {manualDerived.length} manual</span></div><div className="prep-grid derivative-grid">{derivatives.map((asset) => { const changes = transformSummary(asset.transform_override || {}, asset.baked_transform ? derivativeBaseTransform(asset) : {}); return <div className="derivative-card-shell" key={asset.filename}><button className="prep-image-card" onClick={() => openEditor(asset.filename)}><div className="prep-image-wrap"><img src={preparedPreview(asset.filename)} alt={asset.filename} />{changes.length > 0 && <span className="transform-badge">Adjusted</span>}<span className="derived-badge">{asset.origin === "face" ? "Face" : "Manual"}</span></div><div className="prep-image-meta"><strong>{asset.filename}</strong><span>Parent: {asset.parent_filename || "source asset"}</span></div></button><button className="derivative-delete" disabled={busy} onClick={() => removeDerivative(asset)}>×</button></div>; })}</div></>}{selected.size > 0 && <div className="selection-bar"><span><strong>{selected.size}</strong> source image(s) selected</span><div className="actions"><button className="secondary" onClick={() => inclusion(false)}>Exclude</button><button className="primary" onClick={() => inclusion(true)}>Include</button></div></div>}</section>
    <section className="panel stack"><div className="prep-section-heading"><div><p className="eyebrow">Stage 2</p><div className="card-title">Base Composition & Adjustments</div><p className="muted">This is a source-image recipe. Derivatives keep the prepared pixels they were created from.</p></div>{focus && <div className="image-stepper"><button onClick={() => step(-1)}>‹</button><span>{focusIndex + 1} / {workingSources.length}</span><button onClick={() => step(1)}>›</button><strong title={focus.filename}>{focus.filename}</strong></div>}</div>{focusImage && focus && <div className="composition-preview-row"><div className="composition-preview"><img src={globalDirty ? focusImage.image_url : preparedPreview(focus.filename)} alt={focus.filename} style={globalDirty ? { filter: previewFilter(globalDraft) } : undefined} /></div><div className="stack"><div className="form-row"><label>Base aspect<select value={targetAspect} onChange={(event) => setGlobalDraft({ ...globalDraft, aspect_ratio: event.target.value, crop_mode: event.target.value === "source" ? "fit" : "fill" })}>{ASPECTS.map((aspect) => <option key={aspect} value={aspect}>{aspect === "source" ? "Source / no crop" : aspect}</option>)}</select></label><label>Crop mode<select disabled={targetAspect === "source"} value={targetAspect === "source" ? "fit" : globalDraft.crop_mode ?? "fill"} onChange={(event) => setGlobalDraft({ ...globalDraft, crop_mode: event.target.value as "fill" | "fit" })}><option value="fill">Fill / crop</option><option value="fit">Fit / no crop</option></select></label></div><div className="tuning-grid"><SliderField label="Exposure" min={-2} max={2} step={0.1} value={globalDraft.exposure ?? 0} onChange={(value) => setGlobalDraft({ ...globalDraft, exposure: value })} /><SliderField label="Brightness" min={-0.5} max={0.5} step={0.05} value={globalDraft.brightness ?? 0} onChange={(value) => setGlobalDraft({ ...globalDraft, brightness: value })} /><SliderField label="Contrast" min={-0.5} max={0.5} step={0.05} value={globalDraft.contrast ?? 0} onChange={(value) => setGlobalDraft({ ...globalDraft, contrast: value })} /><SliderField label="Gamma" min={0.5} max={1.5} step={0.05} value={globalDraft.gamma ?? 1} onChange={(value) => setGlobalDraft({ ...globalDraft, gamma: value })} /></div></div></div>}<button className="primary" disabled={busy || !globalDirty} onClick={saveGlobal}>{globalDirty ? "Save base composition recipe" : "Base composition saved"}</button></section>
    <section className="panel stack"><div className="prep-section-heading"><div><p className="eyebrow">Stage 3A</p><div className="card-title">Automatic Face Derivatives</div><p className="muted">InsightFace runs on the saved prepared source pixels.</p></div></div><div className="form-row"><label>Derivative aspect<select value={faceAspect} onChange={(event) => setFaceAspect(event.target.value)}>{DERIVATIVE_ASPECTS.map((aspect) => <option key={aspect}>{aspect}</option>)}</select></label><label>Face padding %<input type="number" min="0" max="400" step="10" value={facePadding} onChange={(event) => setFacePadding(Number(event.target.value))} /></label><button className="secondary" disabled={busy || !workingSources.length} onClick={detect}>Detect faces & propose crops</button></div>{faceProposals.length > 0 && <><div className="prep-grid face-proposal-grid">{faceProposals.map((proposal) => <FaceProposalCard key={proposal.id} proposal={proposal} imageUrl={proposal.preview_url || preparedPreview(proposal.filename)} selected={accepted.has(proposal.id)} onToggle={() => setAccepted((current) => { const next = new Set(current); next.has(proposal.id) ? next.delete(proposal.id) : next.add(proposal.id); return next; })} />)}</div><div className="selection-bar face-selection-bar"><div className="actions"><button className="secondary" onClick={() => setAccepted(new Set(faceProposals.map((proposal) => proposal.id)))}>Select all</button><button className="secondary" onClick={() => setAccepted(new Set())}>Deselect all</button></div><span><strong>{accepted.size}</strong> of {faceProposals.length} selected</span><button className="primary" disabled={!accepted.size || busy} onClick={addFaces}>Add selected derivatives</button></div></>}</section>
    <section className="panel stack"><div className="prep-section-heading"><div><p className="eyebrow">Stage 3B</p><div className="card-title">Manual Derivatives</div><p className="muted">The crop editor always starts from the saved prepared source selected in Stage 2.</p></div></div><label>Derivative aspect<select value={manualAspect} onChange={(event) => setManualAspect(event.target.value)}>{DERIVATIVE_ASPECTS.map((aspect) => <option key={aspect}>{aspect}</option>)}</select></label>{focus && <ManualCropEditor imageUrl={preparedPreview(focus.filename)} filename={focus.filename} aspect={manualAspect} busy={busy} onCreate={addManual} />}</section>
    <section className="panel stack training-resolution-panel"><div className="prep-section-heading"><div><p className="eyebrow">Stage 4 · Trainer policy</p><div className="card-title">Maximum Training Resolution</div><p className="muted">One maximum pixel budget, aspect bucketing, and downscale-only by default.</p></div><span className="badge">{modelFamily === "krea2" ? "Krea 2" : "Klein"}</span></div><div className="segmented">{MP_PRESETS.map((mp) => <button key={mp} className={resolutionDraft.max_megapixels === mp ? "selected" : ""} onClick={() => setResolutionDraft({ ...resolutionDraft, max_megapixels: mp })}>{mp} MP</button>)}</div><div className="form-row"><label>Custom maximum MP<input type="number" min=".05" max="8" step=".05" value={resolutionDraft.max_megapixels} onChange={(event) => setResolutionDraft({ ...resolutionDraft, max_megapixels: Number(event.target.value) })} /></label><label className="inline-check"><input type="checkbox" checked={resolutionDraft.enable_bucket} onChange={(event) => setResolutionDraft({ ...resolutionDraft, enable_bucket: event.target.checked })} /> Aspect-ratio bucketing</label><label className="inline-check"><input type="checkbox" checked={resolutionDraft.bucket_no_upscale} onChange={(event) => setResolutionDraft({ ...resolutionDraft, bucket_no_upscale: event.target.checked })} /> Never upscale source detail</label></div><button className="primary" disabled={busy || !resolutionDirty} onClick={saveResolution}>{resolutionDirty ? "Save training resolution policy" : "Training resolution saved"}</button><BucketPreview rows={rows} onReview={review} /></section>
    <section className="panel stack" id="per-image-v4"><div className="prep-section-heading"><div><p className="eyebrow">Stage 5</p><div className="card-title">Per-image Exceptions & Crop Review</div><p className="muted">Cards show the exact saved prepared image. Source exceptions override the global recipe; derivatives start from their baked pixels.</p></div><span className="muted">Global recipe {globalRecipeActive ? "active" : "default"} · {sourceOverrides.length} source exceptions · {derivedOverrides.length} adjusted derivatives</span></div><div className="processed-grid">{included.map((asset) => { const image = dataset.images.find((candidate) => candidate.filename === asset.filename); if (!image) return null; const active = editName === asset.filename, base = inheritedTransform(globalDraft, asset), explicitChanges = transformSummary(asset.transform_override || {}, base), cardLabel = asset.asset_kind === "derived" ? `${asset.origin === "face" ? "Face" : "Manual"} derivative` : explicitChanges.length ? "Source exception" : globalRecipeActive ? "Global recipe" : "Source"; return <div className="processed-grid-item" key={asset.filename}><button className={`processed-card ${active ? "selected" : ""}`} onClick={() => openEditor(asset.filename)}><div className="processed-image fixed-review-card"><img src={preparedPreview(asset.filename)} alt={asset.filename} />{explicitChanges.length > 0 && <span className="transform-badge">Adjusted</span>}</div><div className="prep-image-meta"><strong>{asset.filename}</strong><span>{cardLabel}</span>{explicitChanges.length > 0 && <span className="transform-state-line"><span className="changed">{explicitChanges.join(" · ")}</span></span>}</div></button>{active && <div className="inline-image-editor crop-review-editor"><PositionCropEditor imageUrl={image.image_url} filename={asset.filename} aspect={overrideDraft.aspect_ratio ?? base.aspect_ratio ?? targetAspect} scale={overrideDraft.crop_scale ?? base.crop_scale ?? 1} cropX={overrideDraft.crop_x ?? base.crop_x ?? 0.5} cropY={overrideDraft.crop_y ?? base.crop_y ?? 0.5} filter={previewFilter({ ...base, ...overrideDraft })} onChange={(next) => setOverrideDraft({ ...overrideDraft, crop_x: next.x, crop_y: next.y, crop_scale: next.scale, crop_mode: "fill" })} /><div className="stack inline-editor-controls"><div><p className="eyebrow">Per-image exception</p><div className="card-title">{asset.filename}</div></div><label>Aspect<select value={overrideDraft.aspect_ratio ?? ""} onChange={(event) => setOverrideDraft({ ...overrideDraft, aspect_ratio: event.target.value || undefined, crop_x: 0.5, crop_y: 0.5, crop_scale: 1, crop_mode: event.target.value && event.target.value !== "source" ? "fill" : undefined })}><option value="">Inherit {base.aspect_ratio || targetAspect}</option>{ASPECTS.map((aspect) => <option key={aspect} value={aspect}>{aspect === "source" ? "Source / no crop" : aspect}</option>)}</select></label><div className="tuning-grid"><SliderField label="Exposure" min={-2} max={2} step={0.1} value={overrideDraft.exposure ?? base.exposure ?? 0} onChange={(value) => setOverrideDraft({ ...overrideDraft, exposure: value })} /><SliderField label="Brightness" min={-0.5} max={0.5} step={0.05} value={overrideDraft.brightness ?? base.brightness ?? 0} onChange={(value) => setOverrideDraft({ ...overrideDraft, brightness: value })} /><SliderField label="Contrast" min={-0.5} max={0.5} step={0.05} value={overrideDraft.contrast ?? base.contrast ?? 0} onChange={(value) => setOverrideDraft({ ...overrideDraft, contrast: value })} /><SliderField label="Gamma" min={0.5} max={1.5} step={0.05} value={overrideDraft.gamma ?? base.gamma ?? 1} onChange={(value) => setOverrideDraft({ ...overrideDraft, gamma: value })} /></div><div className="actions">{asset.asset_kind === "derived" && <button className="danger secondary" disabled={busy} onClick={() => removeDerivative(asset)}>Remove derivative</button>}<button className="secondary" onClick={resetOverride}>Reset</button><button className="primary" disabled={!overrideDirty || busy} onClick={saveOverride}>{overrideDirty ? "Save image override" : "Image override saved"}</button></div></div></div>}</div>; })}</div></section>
    <section className="panel stack dataset-final-summary clean-summary"><div className="prep-section-heading"><div><p className="eyebrow">Review</p><div className="card-title">Training Dataset Summary</div><p className="muted">The effective project state that will carry forward into captioning.</p></div><span className="badge">{project.id}</span></div><div className="summary-ledger"><div><span>Working set</span><strong>{included.length} images</strong><small>{workingSources.length} source · {derivatives.length} derived</small></div><div><span>Global source recipe</span><strong>{globalRecipeActive ? "Active" : "Default"}</strong><small>{globalRecipeActive ? transformSummary(prep?.global_transform || {}, DEFAULT_GLOBAL).join(" · ") : "No global tonal/composition change"}</small></div><div><span>Source exceptions</span><strong>{sourceOverrides.length}</strong><small>per-image overrides of global source recipe</small></div><div><span>Derivatives</span><strong>{derivatives.length}</strong><small>{faceDerived.length} face · {manualDerived.length} manual</small></div><div><span>Adjusted derivatives</span><strong>{derivedOverrides.length}</strong><small>changes made after derivative creation</small></div><div><span>Training resolution</span><strong>{resolutionDraft.max_megapixels.toFixed(2)} MP max</strong><small>{down} downscale · {preserved} preserved</small></div><div><span>Expected buckets</span><strong>{groups.length}</strong><small>{groups.map((group) => `${group} (${rows.filter((row) => bucketKey(row) === group).length})`).join(" · ") || "none"}</small></div><div><span>Crop review</span><strong>{cropReview}</strong><small>&gt;5% source area removed</small></div><div><span>Low detail</span><strong>{low}</strong><small>below half target budget</small></div><div><span>Need captions</span><strong>{missing}</strong><small>will surface next</small></div></div><div className="actions"><button className="primary" disabled={!included.length} onClick={() => navigate("/captions")}>Continue to Captions →</button></div></section>
  </div>;
}
